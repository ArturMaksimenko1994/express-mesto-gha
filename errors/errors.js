const ERROR_REQUEST = 400; // переданы некорректные данные.
const ERROR_NOT_FOUND = 404; // карточка или пользователь не найден.
const ERROR_SERVER = 500; // ошибка по-умолчанию.
module.exports = {
  ERROR_REQUEST,
  ERROR_NOT_FOUND,
  ERROR_SERVER,
};
